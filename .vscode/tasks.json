{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "make test",
            "options": {
                "cwd": "${workspaceFolder}/tests"
            },
            "group": {
                "isDefault": true,
                "kind": "build"
            }
        }
    ]
}